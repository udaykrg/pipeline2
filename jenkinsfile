pipeline {
  agent none
  stages {
    stage ('build') {
    agent any
        step {
          sh '''
                  echo "this is build stage"
                  sleep 2
             '''
             }
             }
    stage ('deploy') {
    agent { label "java" }
       steps {
          sh '''
                  echo "this is deploy stage"
                  sleep 2
             '''
             }
    }
    stage ('deploy') {
     agent any
       steps {
          sh '''
                  echo "this is deploy stage"
                  sleep 2
             '''
             }
              }
    stage ('test') {
    agent any
       steps {
          sh '''
                  echo "this is test stage"
                  sleep 2
             '''
             }
    }
  }
}

